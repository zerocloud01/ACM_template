### 线性筛
```cpp
vector<int> pri;
bitset<N> vis;

void get_primes(void)
{
    for(int i=2;i<N;++i)
    {
    	if(!vis[i])	pri.push_back(i);
    	for(int j=0;pri[j]<=N/i;++j)
    	{
    		vis[pri[j]*i] = true;
    		if(i % pri[j] == 0)	break;
    	}
    }
}
```
